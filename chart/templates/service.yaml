apiVersion: v1
kind: Service
metadata:
  name: mcp-github
  namespace: mcp-github
spec:
  type: ClusterIP
  selector:
    app: mcp-github
  ports:
    - name: http
      port: 80
      targetPort: proxy
