# Runtime identity for the Tank-bound GitHub MCP server. The server verifies
# Tank JWT attestations directly and does not need TokenReview permissions.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-github
  namespace: mcp-github
